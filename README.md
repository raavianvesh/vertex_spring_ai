An example project demonstrating usage of vertex ai with spring ai framework.
Uses local stack s3 and dynamodb services along with aws spring cloud to store documents and document data.
A single page application with micro front ends.
Angular for document upload
React to display the uploaded document
Uses graphql to chat with AI
Uses REST API for file operations.
